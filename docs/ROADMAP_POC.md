# PoC 완주 로드맵 — 주 단위 게이트

작성: 2026-07-20 · 전제: 휴가 08-01~09, 발표 09-09(수), 금요일 diff 리추얼
데이터셋 도착 시나리오의 합의 순서(EDA → 표본 100건 전원 읽기 → 기존 파이프라인 실측 → 골든셋 선별) 중 **EDA·실측은 도착 당일 완료됨** — 남은 것은 표본 읽기와 골든셋 선별부터.

## 게이트 원칙

- 한 주 = 게이트 1개. 통과/실패가 숫자로 판정된다.
- 모든 게이트는 `run_eval.py` 또는 명시된 스크립트 실행 결과로만 판정 (사람의 "된 것 같다" 금지).
- 현재 기준선 (2026-07-20 실측): 탐지 F1 1.0 / 판정 Acc 1.0 (골든셋 12건 — **포화이므로 이후 하락이 정상**), Claim 후보 16,464건, KOSIS 후보 820건.

## 주차별 계획

### W1 (07-20~26) — 게이트 **G1 + G-SOURCE-1**

| 판정 기준 | 담당 |
|---|---|
| G1: 클라우드에서 경로 C 실검색, 대표 질의 20건 **Hit@10 실측표 존재** | Evidence(R3) |
| G-SOURCE-1: 파일럿 50건 사람 라벨 vs 자동 분류 — **KOSIS_precision ≥ 0.80**, UNKNOWN 큐·오매핑 목록·`routing_v01.json` 존재 | Evidence(R3)+PM(R1) |
| 부속: 표본 100건 전원 읽기, 골든셋 파일럿 50건 라벨(2인 교차) | 전원, 중재 R1 |
| 부속: HCX 스모크 1회(키 유효성), `detect_llm.py` 초안+Mock | Claim(R2) |
| 부속: 소스분류 배선 — run.py 라우팅+데모 "범위 밖" 표시 | Verdict&Service(R4) |
| 기록: 킥오프 결정·역할 배정 → PROJECT_STATE | Hermes(R5) |

### W2 단축 (07-27~31) — 게이트 **🔒 동결 체크리스트 + HCX 전/후 실측**

| 판정 기준 | 담당 |
|---|---|
| HCX 2차 판별 전/후 탐지 정밀도 diff가 리포트로 존재 (음성 대조군 199건 활용) | R2+R1 |
| 골든셋 50건이 run_eval에 편입되어 **새 기준선 수치가 기록됨** (하락 예상 — 정상) | R1 |
| 동결: 전원 커밋·푸시, 테스트 그린, PROJECT_STATE 스냅샷, 복귀 첫 태스크 예약 | 전원, 취합 R5 |

### 휴가 (08-01~09) — 게이트 없음

### W3 (08-10~16) — 게이트 **EXP-001: 매핑 개선 실측**

| 판정 기준 | 담당 |
|---|---|
| 리부트 반나절: PROJECT_STATE 복기 + 테스트 그린 재확인 | 전원 |
| 경로 A/B/C/D 비교 — 확장 평가셋(50건+)에서 **최고 경로가 기준선 대비 개선 or 동등+비용 절감**을 diff로 증명, 매핑 경로 최종 결정 기록 | R3 실행, R1 판정 |
| NCP 임베딩 v2·리랭커 적용 실험 포함 | R3 |

### W4 (08-17~23) — 게이트 **골든셋 250 + 대량 배치**

| 판정 기준 | 담당 |
|---|---|
| 골든셋 250건 라벨 완료(2인 교차·중재 완료), run_eval 재기준선 | R1 총괄, 전원 라벨 |
| `service_run.py`로 전수 2,649건 배치 처리 완료 — 처리량·실패율·판정 분포 리포트 존재 | R4 |
| 탐지·매핑 오류 유형화 → 규칙 카드 신규 N종(각 테스트 동반) | R2·R3 |

### W5 (08-24~30) — 게이트 **릴리스 게이트 자동 항목 전체 PASS**

| 판정 기준 | 담당 |
|---|---|
| FastAPI 서빙 + HCX 설명 생성(+환각 자동 검사) E2E | R4+R2 |
| `release_gate.py` 자동 게이트 전 항목 PASS (시크릿 스캔 기준을 "추적 여부"로 수정 포함) | R4, 판정 R1 |
| 사람 검토 게이트 5항목 체크 시작 | R1 |

### W6 (08-31~09-06) — 게이트 **09-06 데모 프리즈 진입**

| 판정 기준 | 담당 |
|---|---|
| 최종 지표 확정(run_eval + 대량 배치 리포트), 발표자료·시연 시나리오 완성 | R1 |
| 프리즈: 09-06(일) 이후 버그 수정 외 변경 금지 — sentinel이 감시 | R4 |

### 발표 주간 (09-07~09) — **09-09(수) 최종 발표**

리허설 2회(월·화). 시연 3막: 일치·불일치 판정 → 정직한 회피(잠정치·기준연도·범위 밖) → 플라이휠 실연.

## 금요일 diff 리추얼 (매주 공통)

```bash
PYTHONPATH=. python -m pytest tests/ -q     # 그린 확인
python scripts/run_eval.py                   # diff가 그 주의 성적표
```
R1이 diff를 화면 공유하고, 게이트 통과/실패를 선언하고, 실패 시 원인·대안을 기록한다(Hermes가 ops에 미러).
